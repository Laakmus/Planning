# Supabase (local development)
# Po uruchomieniu `supabase start` użyj kluczy z outputu komendy `supabase status`
SUPABASE_URL=http://127.0.0.1:54331
SUPABASE_ANON_KEY=<your-anon-key-from-supabase-start>
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key-from-supabase-start>

# Public variables (dostępne w kodzie client-side)
PUBLIC_SUPABASE_URL=http://127.0.0.1:54331
PUBLIC_SUPABASE_ANON_KEY=<your-anon-key-from-supabase-start>

# CORS — allowed origin for API requests (no wildcard in production!)
CORS_ORIGIN=http://localhost:4321

# Publiczny bazowy URL aplikacji — używany m.in. do budowy linków aktywacyjnych (invite flow)
PUBLIC_BASE_URL=http://localhost:4321

# Microsoft Graph API (opcjonalne — bez tych zmiennych email działa przez .eml fallback)
# Rejestracja: entra.microsoft.com → App registrations → Web, redirect URI = /api/v1/ms-oauth/callback
# Wymagane uprawnienia (Delegated): Mail.Send, Mail.ReadWrite, offline_access, User.Read
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MS_TENANT=common

# Klucz szyfrujący tokeny Microsoft Graph w tabeli ms_oauth_tokens (pgcrypto pgp_sym_encrypt).
# Min. 16 znaków, zalecane 32+ losowych znaków. NIE commituj prawdziwego klucza.
APP_ENCRYPTION_KEY=your-32-char-encryption-key-here

# Sentry (opcjonalne — bez DSN error tracking wyłączony)
PUBLIC_SENTRY_DSN=
